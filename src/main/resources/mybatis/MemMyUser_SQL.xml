<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MemMyUser">


	<!-- 회원정보 리스트 -->
	<select id="memMylist" resultType="MemMyUserVO">
		SELECT
		ID,
		NAME,
		GENDER,
		EMAIL,
		PHONE,
		BIRTHDAY,
		CAREER
		FROM MEMBER
		ORDER BY ID
	</select>

	<!-- 개인정보 -->
	<select id="memMySelect" resultType="MemMyUserVO" parameterType="String">
		SELECT
		ID,
		NAME,
		GENDER,
		EMAIL,
		PHONE,
		BIRTHDAY,
		CAREER
		FROM MEMBER
		WHERE ID =
		'aaa12'
	</select>

	<!--회원수정  -->
	<select id="memMyUpdate" parameterType="int" resultType="MemUserVO">
		select * from member where id ='aaa12'
	</select>

	<!--회원정보 수정 1-->
	<update id="myUpdate" parameterType="MemMyUserVO">
		UPDATE MEMBER
		SET
		NAME = #{name},
		EMAIL = #{email},
		PHONE = #{phone},
		CAREER = #{career}
		WHERE
		ID = 'aaa12'
	</update>

</mapper>