<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bizJoin">
	<insert id="insertBizJoin" parameterType="BizJoinUserVO">
		<!-- 기업회원가입 -->
		<![CDATA[insert into biz(bsmno,pass,bnum,type,company,email,ceo,phone,field,addr,reg_date)
		         values(#{bsmno},#{pass},(SELECT NVL(MAX(bnum)+1, 1) FROM biz),'B',
		         #{company},#{email},#{ceo},#{phone},#{field},#{addr},to_date(sysdate,'YYYY-MM-DD'))
		         ]]>
	</insert>
		<!-- 사업자번호 중복체크 -->
	<select id="bsm_no_check" resultType="int">
		<![CDATA[select count(*) 
		         from biz 
		         where bsmno=#{bsmno}]]>
	</select>

</mapper>