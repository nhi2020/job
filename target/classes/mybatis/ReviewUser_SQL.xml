<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="reviewUser">

	<select id="reviewList" parameterType="ReviewUserVO" resultType="ReviewUserVO">
		select *
		from (select rownum rn, a.* from (select * from review order by rnum) a) 
		where rn between #{start} and #{end}
	</select>
	 <select id="total" resultType="ReviewUserVO">
		select count(*) from REVIEW
	</select> 

</mapper>
